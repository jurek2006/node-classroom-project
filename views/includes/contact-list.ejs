<!-- contact list element -->
<!-- contacts array is needed -->

<% if(contacts.length > 0) {%>
<ul>
    <% for(let contact of contacts) { %>
    <li>
        <a href="/contact/<%= contact.id %>">
            <%= contact.firstName %> <%= contact.lastName %></a
        >
    </li>
    <% } %>
</ul>
<% } else { %>
<p>There are no contacts yet.</p>
<% } %>
